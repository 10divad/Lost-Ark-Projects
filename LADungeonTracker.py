## HALT TILL WE HAVE API


import discord
import os
import requests
import json

# For list we need - Abyss Dungeon ($), Guardian Raid (GR), Abyss Raid (AR), Legion Raid (LR), BardCDung (BCD), Cubes (CB), Boss Rush (BR), Platinum Field (PF), 

$oreha3


client = discord.Client()

def get_quote


@client.event
async def on_message(message):
    if message.author == client.user:
        return

    if message.content.startswith('$hello'):
        await message.channel.send('Hello')

    # line to start the environment (this is where u put discord server it runs on)
    client.run(os.getenv('TOKEN'))


    def on_message_add(user_input, user):

        if user.message == 


# !playerd
    # output a list of everyones names and what they need

# how would user remove a count?
# would you like to auto remove 1 count in 30min



# command !cube3

# this would be input into a dictionary as: 
    # user_dungeon_count = [david: {name_of_dungeon : count_user_needs},
    # janzen: {name_of_dungeon : count}
    # ]




 
# 
# 
# take user input symbol as type and str as name

# def function that takes 2 above paramaters

    user_input = tostring function - user.message

    try:
        user_input.contains (symbols)
